"""
Synaptix Database — Sub-package.

Provides:
- SQLAlchemy async engine and session factory
- Declarative base with standard columns (id, tenant_id, timestamps)
- Row Level Security tenant context setter
"""
