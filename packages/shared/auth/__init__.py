"""
Synaptix Auth — Sub-package.

Provides:
- JWT encode/decode utilities
- FastAPI dependency injectors (current_user, require_role)
- Tenant context middleware and @require_tenant_context decorator
"""
