# Synaptix — Session 1 — 2026-06-20

## Session Overview

**Date:** 2026-06-20
**Agent acting as:** Orchestrator (00) + Backend (02) + Database (05)
**Duration:** ~60 minutes
**Phase:** 0 → 1A (framework read + first Phase 1A scaffold)

## Work Done

### Environment Setup
- Installed `uv` 0.11.23 (Python package manager)
- Installed Python 3.12.13 (via uv) — pinned in `.python-version`
- Created `.venv` with Python 3.12
- All packages installed: fastapi, sqlalchemy[asyncio], asyncpg, pydantic[email], pydantic-settings, python-jose, passlib, pyotp, structlog, alembic, pytest, ruff, black, mypy, freezegun
- Created `pyproject.toml` at root (monorepo config)
- Created `.python-version` file (pins 3.12)
- Git initialized (`main` branch)
- GitHub repo creation deferred (user to do manually via github.com)

### Packages Built
- `packages/shared/__init__.py`
- `packages/shared/logging.py` — structlog structured logger
- `packages/shared/errors.py` — full SynaptixError hierarchy (30+ error classes)
- `packages/shared/db/__init__.py`
- `packages/shared/db/base.py` — SQLAlchemy 2.0 TenantScopedBase, GlobalBase, mixins
- `packages/shared/db/session.py` — async session + RLS tenant context setter
- `packages/shared/auth/__init__.py`
- `packages/shared/auth/tenant_context.py` — TenantContextMiddleware + @require_tenant_context
- `packages/shared/auth/jwt.py` — JWT create/decode + MFA role enforcement
- `packages/shared/auth/dependencies.py` — get_current_user + require_roles FastAPI deps

### snx-auth Service Scaffold
- `services/snx-auth/pyproject.toml`
- `services/snx-auth/app/__init__.py`
- `services/snx-auth/app/main.py` — FastAPI app with lifespan, middleware, error handlers
- `services/snx-auth/app/config.py` — Settings via pydantic-settings
- `services/snx-auth/app/routers/__init__.py`
- `services/snx-auth/app/routers/auth.py` — All auth endpoints (login, OTP, refresh, MFA, me, logout)
- `services/snx-auth/app/schemas/__init__.py`
- `services/snx-auth/app/schemas/auth.py` — Pydantic v2 request/response models
- `services/snx-auth/app/services/__init__.py`
- `services/snx-auth/app/services/auth_service.py` — Business logic stub with TODOs

### Database Migrations
- `alembic.ini` — monorepo Alembic config
- `services/_migrations/pyproject.toml`
- `services/_migrations/env.py` — async Alembic env
- `services/_migrations/versions/20260620_0001_foundation_tables.py` — Foundation migration (tenants, users, roles, user_roles, audit_log with RLS + JMN seed)

### Tests
- `tests/conftest.py` — shared fixtures (tenant_id, jwt_secret, test_user_id)
- `tests/unit/test_jwt_utils.py` — 20 JWT unit tests (all passing)
- `tests/compliance/test_attendance_thresholds.py` — 23 NMC compliance stubs (all skipped pending implementation)

## Test Results

```
20 passed, 23 skipped in 0.83s
```

All unit tests pass. NMC compliance tests are stubbed (skip, not fail).

## Commands Run

See `docs/COMMAND_HISTORY.md`.

## Next Session Should

1. **Human to do:** Start Docker Desktop → run `docker compose up -d postgres`
2. **Human to do:** Create GitHub repo (private) and push initial commit
3. **Database Agent (05):** Run the migration: `alembic upgrade head`
4. **Backend Agent (02):** Implement `AuthService.login()` (email+password with bcrypt DB lookup)
5. **Backend Agent (02):** Implement `AuthService.request_otp()` (OTP generation + SMS stub)
6. **Backend Agent (02):** Create SQLAlchemy User model in `services/snx-auth/app/models/`
7. **DevOps Agent (09):** Create Dockerfiles for snx-auth service
8. **DevOps Agent (09):** Set up GitHub Actions CI workflow
9. **Flutter installation:** Deferred — install Flutter when ready for Phase 2

## Blockers

- Docker Desktop must be started by Harsh before running `docker compose up -d`
- GitHub repo URL needed before push
