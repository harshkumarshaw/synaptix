[project]
name = "synaptix-migrations"
version = "0.1.0"
description = "Synaptix database migrations"
requires-python = "==3.12.*"

dependencies = [
    "alembic>=1.13",
    "asyncpg>=0.29",
    "sqlalchemy[asyncio]>=2.0",
]
