[project]
name = "snx-institution"
version = "0.1.0"
description = "Synaptix Institution Lifecycle & Profile Service"
requires-python = "==3.12.*"

dependencies = [
    "synaptix-shared",
    "uvicorn[standard]>=0.30",
    "python-dotenv>=1.0",
]
